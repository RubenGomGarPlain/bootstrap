# CONTEXT.md

> Generated by `/at-init-setup` (constitution ceremony) on 2026-06-16. Re-run `/at-init-setup` to update.
> Content outside the AI-TEAM markers is yours to edit freely — it will never be overwritten.
> Content inside the markers is regenerated on each `/at-init-setup` run.

<!-- AI-TEAM:START -->

## Stack

**Solution:** Modular Monolith
**Solution file:** `BootStrap.sln`

**Architecture:** Clean Architecture per module + CQRS (MediatR 12)
- BuildingBlocks: present ✓ (`src/BuildingBlocks/` — CQRS, DomainEvents, Cache, Features primitives)
- Aspire AppHost: present ✓ — `src/AspireHost/` (v13.0.0)

**Modules:**
- `Todos` — core task management bounded context (Domain / Application / Infrastructure)
- `Notifications` — event-driven notification delivery (email + SignalR)
- `Contracts` — shared event types across modules
- `Api` — HTTP entry point; hosts all modules
- `ServiceDefaults` — shared Aspire/OpenTelemetry configuration

**Frontend:** Vue 3 + TypeScript + Vite + Tailwind CSS — `src/spa/`
**Auth:** absent ✗
**IaC:** absent ✗
**CI/CD:** absent ✗

**Runtime dependencies (via Aspire):**
- PostgreSQL (primary store — EF Core 10 + Npgsql, migrations via dbup-postgresql)
- Redis (distributed cache — `Aspire.StackExchange.Redis`)
- RabbitMQ (async messaging — CAP with PostgreSQL outbox + RabbitMQ transport)
- Seq (structured log aggregation — `Aspire.Seq`)

**Observability:** OpenTelemetry (traces, metrics, logs) → Seq + OTLP

**Governance:**
- CONSTITUTION.md: present ✓
- ADRs: present ✓ — 10 records in `docs/adr/`
- Project-local skills: absent ✗

## Hooks

**Deny-hook:** not installed ✗
- Protected paths: `CONSTITUTION.md`, `docs/adr/`
- File: `.github/hooks/guardrails.json`

**Telemetry hook:** disabled ✗

## CodeGraph

**Index:** not initialized ✗
- Path: `.codegraph/`
- Last synced: never
- Stale reminders: disabled ✗

If not initialized, run: `codegraph init -i`

<!-- AI-TEAM:END -->
