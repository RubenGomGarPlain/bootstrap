# CONTEXT.md

> Generated by `/at-init-setup` on 2026-06-16. Re-run `/at-init-setup` to update.
> Content outside the AI-TEAM markers is yours to edit freely — it will never be overwritten.
> Content inside the markers is regenerated on each `/at-init-setup` run.

<!-- AI-TEAM:START -->

## Stack

**Solution:** Modular Monolith
**Solution file:** `BootStrap.sln`

**Architecture:** VSA + CQS
- BuildingBlocks: present ✓ — `src/BuildingBlocks/` (CQRS, Cache, DomainEvents, Diagnostics)
- Aspire AppHost: present ✓ — `src/AspireHost/` (PostgreSQL · Redis · RabbitMQ · Seq · SPA)

**Frontend:** React (Vite) — `src/spa/`
**Auth:** absent ✗ — deferred (see ADR-0007)
**IaC:** absent ✗ — SQL migrations only (`infra/database/migrations/`)
**CI/CD:** present ✓ — GitHub Actions (`ci-dotnet.yml` · `ci-spa.yml`)

**Governance:**
- CONSTITUTION.md: present ✓
- ADRs: present ✓ — 9 records in `docs/adr/`
- Project-local skills: present ✓ — `.claude/skills/` · `.opencode/skills/`

## Hooks

**Deny-hook:** not installed ✗

**Telemetry hook:** disabled ✗

## CodeGraph

**Index:** not initialized ✗
- Path: `.codegraph/`
- Last synced: never
- Pending files: —
- Stale reminders: disabled ✗

If not initialized, run: `codegraph init -i`

<!-- AI-TEAM:END -->
